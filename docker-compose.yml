version: '3.8'
services:
  webssh:
    build: .
    image: webssh:latest
    ports:
      - "8888:8888"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8888
      - AUTH=false      # 设为 true 启用登录认证
      - STORE=false     # 设为 true 启用数据持久化存储
    restart: unless-stopped
